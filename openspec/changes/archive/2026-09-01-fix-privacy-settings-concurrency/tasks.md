## 1. 隐私读取边界

- [x] 1.1 调整 Accessibility 监控流程，在请求焦点元素文本前取得并检查当前应用 bundle identifier。
- [x] 1.2 让排除应用在轮询和设置变更后立即短路，不读取、不翻译其文本，并保持取消与恢复行为正确。
- [ ] 1.3 增加测试验证排除应用不会触发 Accessibility 文本读取，且移除排除项后可恢复处理。

## 2. 翻译速度持久化

- [x] 2.1 在现有设置模型和存储中加入翻译速度的读取、写入及缺省值处理。
- [x] 2.2 将设置页速度控件绑定到持久化设置，并确保运行时翻译协调器使用加载后的值。
- [x] 2.3 增加测试验证三种速度选项可保存、重载，并对无效或缺失值使用安全默认值。

## 3. 并发安全

- [x] 3.1 重构 Accessibility 轮询定时器回调，使快照读取明确在主 actor 上执行并保留现有生命周期控制。
- [x] 3.2 检查停止、重启和连续轮询场景，避免异步回调造成重叠读取或状态竞争。

## 4. 验证与回归

- [x] 4.1 运行 `swift test`，确认现有测试及新增测试全部通过。
- [x] 4.2 运行 macOS `xcodebuild` 构建，确认 actor-isolation 警告消失且项目成功构建。
- [x] 4.3 手动或通过测试核对隐私排除、速度设置跨重启恢复，以及普通应用翻译流程未回归。

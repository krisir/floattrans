对，MVP 应该形成一个**完整可使用的闭环**：

> 中文输入 → 捕获当前句 → 实时翻译 → Overlay 显示英文。

只是把语法、词汇、历史、学习统计这些全部砍掉。下面按“可以直接进入开发”的粒度重新定义 MVP。

# macOS 实时中译英学习工具 — MVP 详细设计

## 1. MVP 定义

### 1.1 一句话描述

用户在 macOS 任意支持的输入框中输入中文，程序自动获取当前正在输入的句子，将其翻译成自然英文，并在屏幕底部实时显示。

核心体验：

```text
用户正在输入：

这个功能我觉得还需要调整一下

                ↓

屏幕底部：

I think this feature still needs some adjustments.
```

用户不需要：

* 复制
* 粘贴
* 按快捷键
* 点击翻译
* 切换应用

整个过程自动完成。

---

# 2. MVP 产品目标

MVP 只验证三个核心假设。

### 假设 A：能否稳定读取输入内容

在常见 macOS App 中，可以知道：

```text
当前哪个输入框正在输入

当前输入框内容

当前光标位置
```

### 假设 B：实时翻译体验是否自然

从用户停止输入到英文出现：

目标：

```text
约 500ms ～ 1s
```

并且不能频繁闪烁。

### 假设 C：Overlay 是否足够低打扰

用户可以持续打开几个小时，而不会影响：

```text
打字
切换 App
点击
滚动
全屏
多显示器
```

---

# 3. MVP 范围

MVP 包含：

| 模块                | MVP |
| ----------------- | --- |
| Menu Bar App      | ✓   |
| Accessibility 权限  | ✓   |
| 当前 App 监听         | ✓   |
| 当前输入框监听           | ✓   |
| 中文文本检测            | ✓   |
| 当前句提取             | ✓   |
| 中文输入法兼容           | ✓   |
| Debounce          | ✓   |
| 中译英               | ✓   |
| 自然英文风格            | ✓   |
| 翻译请求取消 / 去重       | ✓   |
| Bottom Overlay    | ✓   |
| Light / Dark Mode | ✓   |
| 多显示器              | ✓   |
| Pause             | ✓   |
| App 排除            | ✓   |
| 开机启动              | 可选  |
| 历史记录              | ✕   |
| 语法检查              | ✕   |
| 时态分析              | ✕   |
| 生词本               | ✕   |
| OCR               | ✕   |
| AI Tutor          | ✕   |
| 用户账号              | ✕   |
| 云同步               | ✕   |

---

# 4. MVP 完整用户流程

第一次启动：

```text
启动 App
   ↓
Welcome
   ↓
申请 Accessibility Permission
   ↓
选择：
中文 → English
   ↓
选择 Overlay 位置
默认 Bottom
   ↓
完成
   ↓
App 进入 Menu Bar
```

之后正常使用：

```text
用户打开 Slack / Chrome / Notes

                ↓

点击输入框

                ↓

输入：

我觉得我们可以明天继续讨论

                ↓

Accessibility 检测文本变化

                ↓

450ms 没有新输入

                ↓

提取当前句子

“我觉得我们可以明天继续讨论”

                ↓

Translation Engine

                ↓

I think we can continue the discussion tomorrow.

                ↓

屏幕底部 Overlay
```

---

# 5. 整体技术架构

推荐：

```text
Swift
+
SwiftUI
+
AppKit
+
ApplicationServices
```

整体架构：

```text
                macOS Applications
                        │
                        │ Accessibility
                        ▼
             ┌──────────────────────┐
             │ AccessibilityMonitor │
             └──────────┬───────────┘
                        │
                 TextSnapshot
                        │
                        ▼
             ┌──────────────────────┐
             │   InputProcessor     │
             │                      │
             │ • Chinese Detection  │
             │ • Debounce           │
             │ • Sentence Extract   │
             └──────────┬───────────┘
                        │
                    Sentence
                        │
                        ▼
             ┌──────────────────────┐
             │ TranslationService   │
             │                      │
             │ Chinese → English    │
             └──────────┬───────────┘
                        │
                 TranslationResult
                        │
                        ▼
             ┌──────────────────────┐
             │ OverlayCoordinator   │
             └──────────┬───────────┘
                        │
                        ▼
                 Desktop NSPanel

          I think we can discuss it tomorrow.
```

---

# 6. 核心数据流

系统内部只传递几个简单的数据对象。

## TextSnapshot

表示某个时间点用户正在编辑的内容。

```swift
struct TextSnapshot {
    let applicationPID: pid_t

    let bundleIdentifier: String?

    let text: String

    let selectedRange: NSRange?

    let timestamp: Date
}
```

---

## TranslationInput

经过处理后真正需要翻译的内容。

```swift
struct TranslationInput {
    let text: String

    let sourceLanguage: Language

    let targetLanguage: Language

    let requestID: UUID
}
```

例如：

```text
text:
"这个功能我觉得还需要优化"

source:
zh-CN

target:
en
```

---

## TranslationResult

```swift
struct TranslationResult {
    let sourceText: String

    let translatedText: String

    let requestID: UUID
}
```

---

# 7. Accessibility 模块

这是 MVP 最重要的基础设施。

建议：

```text
Accessibility/

PermissionManager.swift

AccessibilityMonitor.swift

FocusedElementObserver.swift

TextElementReader.swift

AccessibilityElementValidator.swift
```

---

# 8. PermissionManager

职责：

检查：

```text
Accessibility Permission
```

没有权限时：

显示：

```text
LinguaFlow needs Accessibility permission
to read the text you're currently editing.

[Open System Settings]
```

必须支持：

```text
permissionGranted

permissionDenied

permissionUnknown
```

App 没有权限时：

不要运行后面的 Text Monitor。

---

# 9. AccessibilityMonitor

MVP 不监听键盘。

不要：

```text
CGEvent keyboard hook
```

也不要：

```text
记录每一个 KeyDown
```

数据来源只有：

> 当前 Accessibility Element 的 Value。

原因非常重要。

假设用户使用拼音输入：

```text
wo jue de
```

系统可能处于：

```text
marked text
```

最后才确认：

```text
我觉得
```

如果监听键盘：

你会得到：

```text
w
o
j
u
e
d
e
```

这不是我们真正需要的数据。

Accessibility 最终看到的是：

```text
我觉得
```

因此：

**Text Field 的 Value 才是 source of truth。**

---

# 10. Focused Element 监听

需要监听两个东西：

```text
当前 Frontmost App

当前 Focused UI Element
```

典型过程：

```text
Chrome

        ↓

用户点击 ChatGPT 输入框

        ↓

FocusedElementChanged

        ↓

获取 Element

        ↓

检查：

Role
Subrole
Editable
Secure
```

支持的元素主要包括：

```text
AXTextField

AXTextArea

AXComboBox

Web editable text element
```

---

# 11. Secure Field 必须跳过

任何安全输入框：

```text
Password
SecureTextField
```

必须：

```text
直接 return
```

禁止：

```text
读取

翻译

缓存

日志记录
```

这是硬性规则。

---

# 12. TextElementReader

获取：

```text
AXValue
```

得到整个输入框：

```text
你好，我想确认一下会议时间。明天下午可以吗？
```

同时尝试获取：

```text
AXSelectedTextRange
```

例如：

```text
你好，我想确认一下会议时间。明天下午可以吗？
                          ↑
                        cursor
```

这样才能正确确定：

> 用户正在编辑哪一句。

---

# 13. MVP 不应该简单翻译整个输入框

错误：

```text
邮件全文：

你好 John，

谢谢你的回复。

我今天看了一下这个问题，目前好像还是有问题。

……

总共 2000 字
```

如果每次用户输入一个字符：

重新翻译：

```text
2000 字
```

不可接受。

必须只提取：

> Current Sentence。

---

# 14. SentenceExtractor

输入：

```text
我们昨天已经讨论过这个问题。我觉得这个方案应该调整一下。你觉得呢？
```

Cursor：

```text
我们昨天已经讨论过这个问题。我觉得这个方案应该调|整一下。你觉得呢？
```

输出：

```text
我觉得这个方案应该调整一下。
```

---

# 15. 中文句子边界

MVP 使用以下字符作为强边界：

```text
。
？
！
\n
```

同时兼容：

```text
.
?
!
```

弱边界可以考虑：

```text
；
;
```

暂时不要使用：

```text
，
,
```

否则：

```text
我觉得这个设计不错，但是还有几个问题需要解决
```

会被错误分开。

---

# 16. 没有完整句号怎么办

真实聊天场景经常是：

```text
我感觉这个方案其实
```

没有句号。

这种情况下：

当前行本身就是 Current Segment。

例如：

```text
今天我测试了一下
```

可以直接翻译：

```text
I tested it today.
```

不要求必须完整成句。

---

# 17. SentenceExtractor fallback

某些 App 可能不给：

```text
SelectedTextRange
```

fallback：

### Level 1

有 cursor：

```text
根据 cursor 找当前句
```

### Level 2

没有 cursor：

```text
取最后一个完整/正在输入的句子
```

### Level 3

仍无法识别：

```text
取当前最后一行
```

同时设最大长度，例如：

```text
300 characters
```

超过则截断。

---

# 18. 中文检测

程序不要对英文输入重复进行翻译。

例如：

```text
Hello
```

不触发。

```text
Hello，我今天有点忙
```

可以触发。

MVP 可以简单使用 Unicode Range 判断。

计算：

```text
ChineseCharacterCount
/
LetterCharacterCount
```

例如：

```text
中文字符 >= 2
```

即可认为需要翻译。

不需要一开始做复杂 Language Detection。

---

# 19. 中文输入法问题

这是 MVP 中需要重点测试的地方。

用户使用拼音输入：

```text
w
wo
wo j
wo jue
```

最终：

```text
我觉得
```

不同 App 对 Accessibility 的暴露方式不同。

可能出现：

### 情况 A

只有候选确认之后：

```text
AXValueChanged
```

最好。

### 情况 B

输入法组合过程中也不断：

```text
AXValueChanged
```

因此必须使用：

```text
Debounce
```

而不是看到 ValueChange 就翻译。

---

# 20. Debounce 策略

推荐默认：

```text
450ms
```

事件：

```text
14:20:01.000
我

14:20:01.080
我觉

14:20:01.150
我觉得

14:20:01.260
我觉得这个

14:20:01.400
我觉得这个方案
```

不断 reset Timer。

直到：

```text
14:20:01.850
```

没有变化。

才触发：

```text
translate(
   "我觉得这个方案"
)
```

---

# 21. 450ms 只是翻译触发，不是 UI 隐藏时间

需要区分两个 Timer：

```text
Input Debounce

450ms
```

和：

```text
Overlay Hide Timer

4s
```

这是完全不同的两个概念。

---

# 22. 输入事件 Pipeline

完整流程：

```text
AXValueChanged
     ↓
Read Text
     ↓
Text changed?
     ↓ no
ignore
     ↓ yes
Create TextSnapshot
     ↓
Debounce 450ms
     ↓
Extract current sentence
     ↓
Contains Chinese?
     ↓ no
hide / ignore
     ↓ yes
Same as last translation?
     ↓ yes
ignore
     ↓ no
TranslationCoordinator.translate()
```

---

# 23. Translation 模块

建议定义协议：

```swift
protocol TranslationEngine {

    func translate(
        _ text: String,
        from source: Language,
        to target: Language
    ) async throws -> String
}
```

这样以后可以随时切换：

```text
Apple Translation

DeepL

Google

OpenAI

自建模型
```

而 UI 完全不用变化。

---

# 24. MVP Translation Provider

推荐第一版：

```text
macOS 15+
Apple Translation Framework
```

这样最大的优势是：

```text
低成本

隐私好

无需 API Key

产品无需维护服务器
```

不过架构上一定不能直接在 UI 中调用 Apple API。

应该：

```text
TranslationCoordinator
        │
        ▼
TranslationEngine
        │
        ▼
AppleTranslationEngine
```

未来：

```text
AppleTranslationEngine
DeepLTranslationEngine
LLMTranslationEngine
```

都只是替换实现。

---

# 25. 翻译风格

MVP 的核心不是：

```text
Literal Translation
```

而是：

```text
Natural English
```

中文：

```text
我晚一点看一下
```

理想：

```text
I'll take a look later.
```

而不是：

```text
I will look at it later.
```

中文：

```text
这个我感觉问题不大
```

更希望：

```text
I don't think this is a big issue.
```

而不是逐字翻译。

---

# 26. 如果使用普通翻译 API

如果翻译 Provider 本身不支持 Style Prompt：

MVP 就接受标准翻译。

不要为了 Natural English 第一版直接把系统做复杂。

架构预留：

```swift
enum TranslationStyle {
    case natural
}
```

UI 第一版甚至不需要暴露这个选项。

内部默认为：

```text
Natural
```

---

# 27. TranslationCoordinator

这是实时翻译稳定性的核心。

必须解决：

> 老请求比新请求晚回来。

例如：

用户先输入：

```text
我觉得
```

Request A。

继续输入：

```text
我觉得这个方案挺好的
```

Request B。

因为网络问题：

```text
B 先返回
A 后返回
```

如果不处理：

Overlay 会：

```text
I think this approach is pretty good.

↓

I think.
```

这是严重 Bug。

---

# 28. Request Generation

每次翻译生成：

```swift
UUID
```

或者自增：

```swift
generationID
```

例如：

```text
Request 103
我觉得

Request 104
我觉得这个功能很好
```

现在：

```text
latestGeneration = 104
```

即使 103 最后返回：

```text
103 != 104

ignore
```

绝对不能更新 UI。

---

# 29. Cancellation

如果 Translation API 支持 Swift Concurrency：

保存：

```swift
Task
```

例如：

```swift
translationTask?.cancel()
```

新请求进来时：

```text
Cancel old request
```

然后创建新的。

但即使支持 Cancellation：

仍然必须有：

```text
generation check
```

双保险。

---

# 30. 翻译缓存

短时间内用户可能：

```text
输入一句
删除
重新输入
```

可以做简单：

```text
LRU Cache
```

例如：

```text
50 entries
```

Key：

```text
normalizedSourceText
+
sourceLanguage
+
targetLanguage
```

Value：

```text
translatedText
```

这样：

```text
这个设计挺好的
```

第二次出现：

无需再次翻译。

---

# 31. 输入文本 normalization

翻译前：

```text
trim whitespace
```

把：

```text
"   我觉得可以   "
```

处理为：

```text
"我觉得可以"
```

但不要：

```text
修改标点

修改用户内容

自动纠错中文
```

翻译系统看到什么，就翻译什么。

---

# 32. Overlay 架构

Overlay 推荐：

```text
NSPanel
+
NSHostingView
+
SwiftUI
```

不要使用普通 SwiftUI Window。

需要系统级控制：

```text
floating

non-activating

join all spaces

fullscreen auxiliary

mouse passthrough
```

---

# 33. Overlay Window

结构：

```text
OverlayWindowController
        │
        ▼
TranslationPanel : NSPanel
        │
        ▼
NSHostingView
        │
        ▼
TranslationOverlayView
```

---

# 34. NSPanel 行为

核心要求：

```text
不成为 Key Window

不成为 Main Window

不抢输入焦点

鼠标穿透

切换 App 不消失

切换 Space 仍然存在

全屏 App 可以出现
```

核心配置方向：

```swift
panel.level = .floating

panel.collectionBehavior = [
    .canJoinAllSpaces,
    .fullScreenAuxiliary
]

panel.isOpaque = false

panel.backgroundColor = .clear

panel.ignoresMouseEvents = true
```

并使用 non-activating panel。

---

# 35. MVP Overlay 只做 Bottom Subtitle

第一版不要做：

```text
Near Input Field
Top Left
Custom Position
Drag
```

只做：

> Bottom Center。

这样显著降低复杂度。

---

# 36. Overlay UI

默认：

```text
                I'll take a look later.
```

不显示中文。

因为中文已经在用户自己的输入框中。

视觉结构：

```text
┌─────────────────────────────────────────────┐
│                                             │
│        I'll take a look later.              │
│                                             │
└─────────────────────────────────────────────┘
```

实际：

背景弱化到接近字幕。

---

# 37. Overlay 尺寸

推荐：

```text
Min Width
360px

Preferred Width
520px

Max Width
720px
```

Padding：

```text
Horizontal
24px

Vertical
14px
```

圆角：

```text
14px
```

---

# 38. 字体

英文翻译：

```text
SF Pro

18px

Medium

line-height ≈ 24px
```

不要过小。

这是学习型产品。

用户应该能够：

> 非常轻松扫一眼就读到英文。

---

# 39. Overlay 位置

当前目标屏幕：

```text
focused input element 所在 Screen
```

如果无法确定：

```text
frontmost window 所在 Screen
```

仍失败：

```text
NSScreen.main
```

Overlay：

```text
x =
screen.midX - overlay.width / 2
```

y：

```text
screen.visibleFrame.minY + 48
```

注意使用：

```text
visibleFrame
```

而不是：

```text
frame
```

否则可能和 Dock 冲突。

---

# 40. 多显示器

例如：

```text
MacBook
        +
External Monitor
```

用户在外接屏 Slack 输入。

Overlay 必须：

```text
外接屏底部
```

而不是固定主显示器。

因此每次：

```text
FocusedElementChanged
```

需要重新计算目标屏幕。

---

# 41. Overlay 状态机

建议只有：

```text
Hidden

Translating

Visible
```

但 UI 不需要一直表现 Translating。

状态：

```text
Hidden
```

没有需要显示的内容。

---

用户停止输入：

```text
Translating
```

如果翻译：

```text
< 800ms
```

什么都不显示。

避免 loading 闪一下。

---

超过：

```text
800ms
```

可以显示：

```text
…
```

但甚至 MVP 可以完全不显示 loading。

---

结果返回：

```text
Visible
```

显示英文。

---

# 42. Overlay 动画

出现：

```text
Opacity
0 → 1

150ms
```

位置：

```text
Y + 4
→
Y
```

不要：

```text
Scale

Bounce

Spring
```

这是系统辅助工具。

动画应该非常安静。

---

# 43. 翻译更新时不要整个 Overlay 消失

用户：

```text
我觉得
```

出现：

```text
I think...
```

继续：

```text
我觉得这个方案挺好的
```

翻译变成：

```text
I think this approach is pretty good.
```

Overlay：

保持存在。

只更新文字。

不要：

```text
fade out
fade in
```

否则一直闪。

---

# 44. Overlay 隐藏策略

用户停止输入后：

翻译出现。

然后：

```text
4 秒没有新的输入
```

Fade Out：

```text
200ms
```

隐藏。

如果用户在 4 秒内继续输入：

Cancel Hide Timer。

保持 Overlay。

---

# 45. 一个完整时间线

例如：

```text
10:00:00.000

用户：
我
```

```text
10:00:00.100

我觉
```

```text
10:00:00.260

我觉得
```

```text
10:00:00.500

我觉得这个方案不错
```

停止。

Debounce：

```text
450ms
```

所以：

```text
10:00:00.950
```

创建翻译请求。

例如翻译耗时：

```text
250ms
```

于是：

```text
10:00:01.200

Overlay:

I think this approach looks good.
```

整体：

从最后一个字：

```text
700ms
```

即可看到结果。

这是非常理想的体验。

---

# 46. Overlay 内容最大长度

不要让 Overlay 变成一个巨大的字幕框。

最大输入：

```text
300 Chinese characters
```

推荐 Current Segment：

```text
< 120 Chinese characters
```

超过：

优先找最近的：

```text
。！？换行
```

如果真的特别长：

截取 cursor 前后的必要部分。

---

# 47. App Exclusion

需要一个：

```text
ExcludedAppsManager
```

至少默认排除：

```text
1Password

Keychain Access
```

用户可以 Settings：

```text
Excluded Applications

1Password

+ Add App
```

如果当前 App：

```text
bundleID ∈ excludedApps
```

Input Pipeline：

直接停止。

---

# 48. Menu Bar

MVP Menu Bar：

```text
┌────────────────────────────┐
│ Live English          ● On │
├────────────────────────────┤
│ 中文 → English             │
│                            │
│ Pause                      │
│                            │
├────────────────────────────┤
│ Settings...                │
│ Quit                       │
└────────────────────────────┘
```

不要有更多功能。

---

# 49. Pause

点击：

```text
Pause
```

立即：

```text
停止 Accessibility Input Processing

Cancel Translation Task

Hide Overlay
```

Menu Bar：

```text
Live English       Paused
```

再次点击：

```text
Resume
```

---

# 50. Settings MVP

Settings 只需要三个 Section。

## General

```text
Enable Live Translation        ON

Launch at Login                ON/OFF
```

---

## Translation

```text
Input Language

Chinese

Output Language

English


Translation Delay

○ Fast        300 ms

● Balanced    450 ms

○ Relaxed     700 ms
```

MVP 可以固定：

```text
Chinese → English
```

甚至不开放修改。

---

## Overlay

```text
Position

Bottom Center
```

第一版固定。

允许：

```text
Text Size

Small
Medium
Large
```

以及：

```text
Hide After

3 sec
5 sec
Never
```

---

# 51. Onboarding

MVP 只需要三步。

### Step 1

```text
Write in Chinese.
See it in English.

Live English translates what you're typing
without interrupting your workflow.

[Continue]
```

---

### Step 2

```text
Accessibility Permission

Live English needs Accessibility permission
to read the text in the field you're
currently editing.

It never reads password fields.

[Allow Permission]
```

---

### Step 3

直接展示：

```text
Try it now

Type something in Chinese:

┌────────────────────────────────┐
│ 我明天会继续处理这个问题        │
└────────────────────────────────┘


I'll continue working on this tomorrow.
```

这个输入框可以直接使用自己的 SwiftUI TextField。

用于告诉用户：

> 产品现在已经正常工作。

---

# 52. 内部状态

整个 App 可以维护一个：

```swift
@MainActor
final class AppState: ObservableObject {

    var isEnabled: Bool

    var permissionState: PermissionState

    var focusedApplication: ApplicationInfo?

    var currentText: String?

    var currentSentence: String?

    var translationState: TranslationState

    var overlayState: OverlayState
}
```

不要把全部逻辑塞进去。

AppState 只负责：

> 当前全局状态。

---

# 53. 模块职责

推荐结构：

```text
LiveEnglish/

App/
    LiveEnglishApp.swift
    AppDelegate.swift
    AppState.swift

Accessibility/
    AccessibilityPermissionManager.swift
    AccessibilityMonitor.swift
    FocusedElementObserver.swift
    TextElementReader.swift

Input/
    InputCoordinator.swift
    TextSnapshot.swift
    SentenceExtractor.swift
    ChineseTextDetector.swift
    InputDebouncer.swift

Translation/
    TranslationCoordinator.swift
    TranslationEngine.swift
    AppleTranslationEngine.swift
    TranslationCache.swift

Overlay/
    OverlayCoordinator.swift
    TranslationPanel.swift
    TranslationOverlayView.swift
    ScreenResolver.swift

MenuBar/
    MenuBarView.swift

Settings/
    SettingsView.swift
    SettingsStore.swift

Onboarding/
    OnboardingView.swift

Privacy/
    ExcludedAppsManager.swift
```

---

# 54. Coordinator 关系

不要让模块互相乱调用。

推荐：

```text
AccessibilityMonitor

        ↓

InputCoordinator

        ↓

TranslationCoordinator

        ↓

OverlayCoordinator
```

由：

```text
AppCoordinator
```

连接：

```swift
final class AppCoordinator {

    let accessibilityMonitor: AccessibilityMonitor

    let inputCoordinator: InputCoordinator

    let translationCoordinator: TranslationCoordinator

    let overlayCoordinator: OverlayCoordinator
}
```

---

# 55. InputCoordinator

它是 Input Pipeline 的核心。

伪代码：

```swift
func handle(snapshot: TextSnapshot) {

    guard appIsEnabled else {
        return
    }

    guard !excludedApps.contains(snapshot.bundleIdentifier) else {
        return
    }

    debouncer.submit(snapshot) { snapshot in

        let sentence = sentenceExtractor.extract(from: snapshot)

        guard !sentence.isEmpty else {
            return
        }

        guard chineseDetector.containsChinese(sentence) else {
            return
        }

        guard sentence != lastSubmittedSentence else {
            return
        }

        lastSubmittedSentence = sentence

        onSentenceReady(sentence)
    }
}
```

---

# 56. TranslationCoordinator

伪代码：

```swift
func translate(_ text: String) {

    translationTask?.cancel()

    generation += 1

    let currentGeneration = generation

    translationTask = Task {

        if let cached = cache[text] {

            await publish(
                cached,
                generation: currentGeneration
            )

            return
        }

        do {

            let result = try await engine.translate(
                text,
                from: .chinese,
                to: .english
            )

            guard !Task.isCancelled else {
                return
            }

            guard currentGeneration == generation else {
                return
            }

            cache[text] = result

            await publish(
                result,
                generation: currentGeneration
            )

        } catch {

            // MVP 静默失败
        }
    }
}
```

---

# 57. OverlayCoordinator

只接受：

```text
最终有效翻译
```

接口：

```swift
func show(
    translation: String,
    on screen: NSScreen
)
```

和：

```swift
func hide()
```

TranslationCoordinator 不应该知道：

```text
NSPanel
SwiftUI
Animation
Screen
```

---

# 58. 事件完整时序

```text
User

 │ 中文输入
 ▼

macOS Input Method

 │
 ▼

Focused Text Element

 │ AXValueChanged
 ▼

AccessibilityMonitor

 │ TextSnapshot
 ▼

InputCoordinator

 │
 │ 450ms debounce
 │
 │ sentence extraction
 │
 │ Chinese detection
 ▼

Current Sentence

 │
 ▼

TranslationCoordinator

 │
 │ cancel previous
 │
 │ request generation
 ▼

TranslationEngine

 │
 ▼

English

 │ generation valid?
 ▼

OverlayCoordinator

 │
 ▼

NSPanel

 │
 ▼

User sees English
```

---

# 59. 错误策略

MVP 原则：

> 尽可能静默失败。

翻译失败：

不要：

```text
Alert
```

不要：

```text
Notification
```

不要：

```text
Translation API Error 500
```

最多：

Overlay 显示：

```text
Translation unavailable
```

但我甚至建议 MVP：

> 直接不显示。

Menu Bar 可以有：

```text
Translation unavailable
```

方便 debug。

---

# 60. Accessibility 读取失败

某些 App：

```text
AXValue unsupported
```

直接 ignore。

不要影响系统。

开发 Debug Build：

Console：

```text
Unsupported element:
role=AXGroup
bundle=com.xxx
```

Release：

不输出用户文本。

---

# 61. 日志隐私

非常重要。

开发阶段容易：

```swift
print(text)
```

但 Release Build 必须禁止：

```text
原始中文

翻译内容

密码

输入历史
```

日志只能：

```text
bundleID

event type

text length

latency

error code
```

例如：

```text
translation_success
length=18
latency=324ms
```

而不是：

```text
text=我今天不想上班
```

---

# 62. MVP 性能目标

Accessibility：

```text
基本接近 0 CPU
```

因为：

事件驱动。

不要：

```text
100ms polling
```

翻译：

每个自然输入 pause：

```text
1 request
```

不是：

```text
1 character
=
1 request
```

---

# 63. 关键性能指标

建议开发期间记录：

```text
InputEvent → Debounce Fire

Debounce Fire → Translation Start

Translation Start → Translation Complete

Translation Complete → Overlay Visible
```

最重要指标：

```text
Last Input
→
Overlay Visible
```

目标：

```text
P50 < 800ms

P95 < 1500ms
```

如果使用本地翻译：

最好做到：

```text
P50 < 600ms
```

---

# 64. MVP 兼容性目标

第一阶段正式支持：

```text
TextEdit

Apple Notes

Safari

Chrome
```

第二梯队测试：

```text
Slack

Discord

Notion

VS Code
```

不要对外宣传：

```text
Works everywhere
```

应该是：

> Works with most standard macOS text fields.

因为部分：

```text
Canvas editor

Custom rendering

Terminal

游戏输入框
```

Accessibility 暴露可能不完整。

---

# 65. TextEdit 验收

输入：

```text
今天下午我需要处理几个问题
```

预期：

```text
I need to deal with a few issues this afternoon.
```

Overlay：

底部出现。

继续：

```text
今天下午我需要处理几个问题，然后准备明天的会议
```

Overlay 更新。

输入焦点始终保持：

```text
TextEdit
```

---

# 66. Chrome 验收

Chrome：

打开任意：

```text
textarea
```

输入：

```text
我觉得我们应该重新设计这个页面
```

预期：

```text
I think we should redesign this page.
```

用户点击网页按钮：

Overlay 自动消失。

重新点击输入框：

继续工作。

---

# 67. 中文输入法验收

拼音输入：

```text
wo jue de zhe ge she ji bu cuo
```

候选：

```text
我觉得这个设计不错
```

翻译 API 请求次数：

目标：

```text
1～2 次
```

不能：

```text
20 次
```

---

# 68. 快速输入测试

用户不停输入：

```text
我觉得这个方案还可以但是我们可能还需要优化一下具体实现
```

持续：

```text
3 秒
```

这 3 秒内：

最好：

```text
0～2 个请求
```

而不是几十个。

---

# 69. 删除文本测试

用户：

```text
我觉得这个功能挺不错的
```

出现：

```text
I think this feature is pretty good.
```

然后全部删除。

必须：

```text
Cancel Translation

Hide Overlay
```

不能继续显示旧翻译。

---

# 70. 切换 App 测试

Chrome 输入：

```text
这个设计不错
```

翻译请求刚开始。

立即切到 Notes：

输入：

```text
明天继续
```

如果 Chrome 结果晚返回：

绝对不能覆盖：

```text
I'll continue tomorrow.
```

这里依赖：

```text
request generation
+
focused session identity
```

---

# 71. Focus Session

建议定义：

```swift
struct InputSessionID: Hashable {
    let pid: pid_t
    let elementIdentifier: String
}
```

如果无法稳定获取 Element ID：

内部给 AX Element 创建 session token。

焦点变化：

```text
Session A
→
Session B
```

旧 Session 的所有：

```text
debounce
translation
overlay update
```

失效。

---

# 72. Overlay 多屏测试

Chrome：

Display A。

Notes：

Display B。

从 Chrome 切 Notes：

Overlay：

```text
Display A
→
Display B
```

不能留在旧屏幕。

---

# 73. 全屏测试

Chrome Full Screen。

输入中文。

Overlay 必须：

```text
仍可显示
```

但不能覆盖：

```text
Dock
```

并尽可能避开：

```text
系统字幕区域
```

第一版统一：

```text
Bottom + 48px
```

即可。

---

# 74. MVP UI 页面总数

真正需要开发的 UI 只有：

```text
01 Welcome

02 Accessibility Permission

03 Test Translation

04 Menu Bar

05 Settings

06 Bottom Overlay

07 Permission Missing State
```

只有：

> 7 个主要 UI 状态。

非常适合快速开发。

---

# 75. MVP 开发 Iteration 1

目标：

**Text Input → Console**

实现：

```text
Accessibility Permission

Focused App

Focused Element

AXValueChanged

TextSnapshot
```

验收：

TextEdit 输入：

```text
你好
```

Console：

```text
currentTextLength=2
```

Debug 模式允许临时打印文本。

---

# 76. MVP 开发 Iteration 2

目标：

**Text → Current Sentence**

加入：

```text
Cursor

SentenceExtractor

ChineseDetector

Debounce
```

验收：

输入：

```text
第一句话。第二句话正在输入
```

输出：

```text
第二句话正在输入
```

---

# 77. MVP 开发 Iteration 3

目标：

**Current Sentence → English**

实现：

```text
TranslationEngine

TranslationCoordinator

Cancellation

Generation

Cache
```

验收：

```text
我晚点看一下

↓

I'll take a look later.
```

快速连续输入不会出现旧结果覆盖。

---

# 78. MVP 开发 Iteration 4

目标：

**Translation → Desktop Overlay**

实现：

```text
NSPanel

SwiftUI View

Bottom Position

Fade

Mouse Passthrough

Multi Screen
```

最终第一次形成：

```text
中文输入

↓

Desktop English Subtitle
```

完整产品闭环。

---

# 79. MVP 开发 Iteration 5

目标：

**真正可以每天运行**

增加：

```text
Menu Bar

Pause

Settings

Excluded Apps

Permission Error

Launch at Login
```

完成后：

就已经是一个可以自己长期使用的 V0.1。

---

# 80. 最终 MVP 验收标准

MVP Release 的定义不是：

> 功能代码全部写完。

而是完成下面这一条完整体验：

用户启动 App。

授权 Accessibility。

关闭主窗口。

程序常驻 Menu Bar。

打开 Chrome。

点击 ChatGPT 输入框。

输入：

```text
我觉得这个功能还可以再优化一下
```

用户停下输入。

约 0.5～1 秒之后：

屏幕底部自然出现：

```text
I think this feature could still be improved.
```

用户继续输入：

```text
特别是交互体验这一部分
```

英文自然更新为：

```text
Especially the interaction experience.
```

整个过程：

```text
没有复制粘贴

没有快捷键

没有切换 App

没有抢走焦点

没有明显闪烁

没有大量 API 请求
```

切换到 Notes：

继续有效。

切换外接显示器：

Overlay 跟随。

输入密码：

完全不处理。

暂停 App：

立即停止。

这就是 MVP 完成。

---

# 81. MVP 最核心的工程原则

整个实现始终围绕下面这条 Pipeline：

```text
Focused Editable Element
        ↓
Text Snapshot
        ↓
Current Chinese Sentence
        ↓
Stable Input
        ↓
Natural English
        ↓
Non-intrusive Overlay
```

其中真正需要优先投入时间打磨的不是设置页，也不是动画，而是三件事情：

**第一，Current Sentence 是否取对。**

如果取错，翻译再好也没有意义。

**第二，什么时候触发翻译。**

太快会闪，太慢没有实时感。

**第三，旧结果绝不能覆盖新结果。**

这决定整个产品有没有“稳定、安静、像系统原生能力一样”的感觉。

第一版只要把这三点 + Bottom Overlay 做好，就已经形成了一个非常完整的产品。

